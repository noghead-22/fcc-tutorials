/**
 * let a variable cannot be declared twice with the same name
 * 
 * variables declared using const are read-only.
 * that means once a value assigned to a const variable value can't be changed.
 * that means once a variable assigned with const , it cannot be reassigned
 */



const hi = 2;
hi = 3;
console.log(hi);